<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="digest.digestandroid">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".api.VolleySingleton"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ViewTopicActivity"
            android:exported="true"
            android:label="@string/view_topic_label"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SignupActivity"
            android:exported="true"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".ForgetPasswordActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".ViewRegisteredHomeActivity"
            android:label="@string/home_label"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".ViewSearchActivity"
            android:label="Search View Activity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".CreateTopicFragmentsActivity"
            android:exported="true"
            android:label="@string/create_topic_label"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".AddEntityActivity"
            android:label="@string/add_entity_label"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".AddCommentActivity"
            android:label="@string/add_comment_label"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".AddMaterialPopActivity"
            android:label="@string/add_material_label"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".AddQuestionActivity"
            android:label="Add Question"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".AddChannelActivity"
            android:label="Add Channel"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".AdvancedSearchActivity"
            android:label="Advanced Search"
            android:theme="@style/Theme.AppCompat.DayNight.Dialog" />
        <activity
            android:name=".ViewChannelTopicsActivity"
            android:label="Channel"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
    </application>

</manifest>